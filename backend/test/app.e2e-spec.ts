/**
 * Legacy e2e placeholder — pełne testy HTTP bez bazy są w test/integration/api.integration-spec.ts
 */
describe('App (e2e legacy)', () => {
  it('delegates to integration suite', () => {
    expect(true).toBe(true);
  });
});
